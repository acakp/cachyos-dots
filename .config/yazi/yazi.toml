[mgr]
show_hidden = true
ratio = [3, 6, 11]

[opener]
play = [
  { run = "mpv %s", orphan = true, for = "unix" },
]
video = [
  { run = "showtime %s", orphan = true, for = "unix"}
]

[open]
prepend_rules = [
  { mime = "video/*", use = "video"},
  { mime = "audio/*", use = "play"},
]
